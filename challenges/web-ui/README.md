# Web / UI Challenges

Frontend/UI/UX, performance, accessibility.

- Copy `../template.md` → `challenge-<slug>.md`.

### Gợi ý chủ đề
- SPA performance (bundle split, lazy load, image opt).
- Accessibility (ARIA, keyboard nav, color contrast).
- State management, forms, error handling.
- E2E test (Playwright/Cypress).